// component
import SectionTitle from "components/section-title/SectionTitle"

export default () => {
  return (
    <section className="about">
      <SectionTitle title="About me" subtitle="My skills and what I do" />
    </section>
  )
}